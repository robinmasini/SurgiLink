export default function Dashboard() {
    return (
        <div style={{ padding: '50px', background: '#F0F9FF', minHeight: '100vh' }}>
            <h1>Dashboard DEBUG MODE</h1>
            <p>If you see this, the Dashboard is rendering correctly.</p>
        </div>
    );
}
